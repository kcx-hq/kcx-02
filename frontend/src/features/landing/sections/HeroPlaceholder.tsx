export function HeroPlaceholder() {
  return (
    <section className="mt-6 min-h-[65vh] rounded-lg border border-border bg-background p-8">
      <p className="text-xs font-semibold uppercase tracking-[0.14em] text-muted-foreground">
        Hero Placeholder
      </p>
      <p className="mt-3 max-w-2xl text-sm text-muted-foreground">
        Base layout is ready for the real Header + Hero implementation in the next step.
      </p>
    </section>
  )
}
