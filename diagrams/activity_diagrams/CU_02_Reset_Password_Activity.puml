@startuml
title CU_02 Reset Password - Activity
start
:ClientUser opens reset link;
:Enter new password;
if (Reset token expired?) then (Yes)
  :Show token expired message;
  :Request new reset link;
  stop
else (No)
  :Apply new password;
  :Mark reset token used;
  :Show success confirmation;
  stop
endif
@enduml